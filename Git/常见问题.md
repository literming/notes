## 已经被追踪的文件/文件夹不在追踪
在.gitignore添加忽略规则之后，还需要自行以下命令
``` bash
# 文件
git rm --cached [file]
# 文件夹
git rm -r --cached [folder]
```

``` bash
# 统一文件为 LF 换行符
$ echo "* text=auto eol=lf" >> .gitattributes
$ git rm --cached -r . #清空GIT索引缓存，但不删除文件
$ git add .
$ git commit . -m "统一文件为 LF 换行符"
```



